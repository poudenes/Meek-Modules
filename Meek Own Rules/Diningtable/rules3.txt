On touch2#touch2=1 do
  timerSet,1,2
endon

on rules#timer=1 do
 if [touch2#touch2]=0
    Publish homeassistant/meek/diningtable,1
    neopixel,2,0,255,0 // GREEN
 else
    Publish homeassistant/meek/diningtable,1
    Publish homeassistant/meek/diningtable,1
    neopixel,2,0,0,255 // BLUE
 endif
on rules#timer=1 do
 if [touch2#touch2]=1
    Publish homeassistant/meek/diningtable,0
    neopixel,2,255,0,0 // RED
 endif
endon
